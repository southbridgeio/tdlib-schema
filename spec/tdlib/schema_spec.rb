# frozen_string_literal: true

RSpec.describe Tdlib::Schema do
  it "has a version number" do
    expect(Tdlib::Schema::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
